import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/providers/theme-provider";
import { Toaster } from "sonner";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "VoiceCart – AI Voice Shopping Assistant",
  description:
    "Shop smarter with voice commands. An AI-powered shopping assistant that understands natural language, suggests products, and manages your list hands-free.",
  keywords: [
    "voice shopping",
    "AI shopping assistant",
    "voice commands",
    "shopping list",
    "NLP shopping",
  ],
  authors: [{ name: "VoiceCart Team" }],
  openGraph: {
    title: "VoiceCart – AI Voice Shopping Assistant",
    description: "Shop smarter with voice commands.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className={inter.variable}>
      <body className="font-sans antialiased">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem={false}
          disableTransitionOnChange
        >
          {children}
          <Toaster
            position="top-right"
            toastOptions={{
              style: {
                background: "var(--card)",
                color: "var(--foreground)",
                border: "1px solid var(--border)",
              },
            }}
          />
        </ThemeProvider>
      </body>
    </html>
  );
}
